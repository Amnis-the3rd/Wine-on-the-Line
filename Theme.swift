import SwiftUI

enum AppTheme {
    // Primary wine palette
    static let burgundy = Color(red: 0.45, green: 0.08, blue: 0.15)
    static let wine = Color(red: 0.55, green: 0.12, blue: 0.20)
    static let rosé = Color(red: 0.85, green: 0.45, blue: 0.50)
    static let champagne = Color(red: 0.96, green: 0.91, blue: 0.82)
    static let gold = Color(red: 0.80, green: 0.68, blue: 0.38)

    // Metro line colors
    static let metroGreen = Color(red: 0.13, green: 0.62, blue: 0.33)
    static let metroRed = Color(red: 0.85, green: 0.18, blue: 0.22)
    static let metroBlue = Color(red: 0.15, green: 0.35, blue: 0.70)

    // Neutrals
    static let cardBackground = Color(.systemBackground)
    static let secondaryBackground = Color(.secondarySystemBackground)
    static let subtleText = Color(.secondaryLabel)
}
